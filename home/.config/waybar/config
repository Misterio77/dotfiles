{
    "layer": "bottom",
    "height": 42,
    "modules-left": ["sway/workspaces", "sway/mode", "custom/player", "tray"],
    "modules-center": ["sway/window"],
    "modules-right": [ "custom/eth", "custom/gamemode", "custom/flavours", "pulseaudio", "network", "cpu", "memory", "temperature#cpu", "temperature#gpu", "clock"],
    "margin-left": 0,
    "margin-right": 0,
    "margin-top": 0,
    "sway/window": {
        "max-lenth": 20
    },
    "custom/flavours": {
        "exec": "flavours current && echo Flavours scheme",
        "interval": 5,
        "on-click": "old=\"$(flavours current)\" && flavours list -l | alacritty-fzf --preview='flavours apply -l {}' || flavours apply \"${old}\" && flavours apply \"$(flavours current)\"",
        "on-click-right": "darkmode",
        "signal": 7,
        "format": "{} "
    },
    "custom/gamemode": {
        "exec": "(gamemoded --status | grep 'is active' -q) && echo '\n Gamemode is active' || echo ''",
        "interval": 2
    },
    "custom/player": {
        "exec-if": "[[ \"$(player=$(preferredplayer) && mpris-ctl --player $player status)\" != \"Stopped\" ]]",
        "exec": "player=$(preferredplayer) && mpris-ctl --player $player info '{\"text\": \"(%player_name) %artist_name - %track_name\",\"alt\": \"%play_status\"}'",
        "max-length": 35,
        "return-type": "json",
        "interval": 2,
        "signal": 8,
        "on-click": "player=$(preferredplayer) && mpris-ctl --player $player pp; pkill -SIGRTMIN+8 waybar",
        "on-scroll-up": "player=$(preferredplayer) && mpris-ctl --player $player previous; pkill -SIGRTMIN+8 waybar",
        "on-scroll-down": "player=$(preferredplayer) && mpris-ctl --player $player next; pkill -SIGRTMIN+8 waybar",
        "format": "{icon}   {}",
        "format-icons": {
            "Playing": "契",
            "Paused": "",
            "Stopped": "栗"
        }
    },
    "custom/eth": {
        "exec-if": "systemctl --user is-active ethminer",
        "exec": "journalctl --user -n 10 -u ethminer | grep -e '\\ m\\ .*' | cut -d ' ' -f12-13; echo ' '",
        "interval": 1,
        "on-click": "alacritty --class AlacrittyFloatingMiner -e journalctl --user -u ethminer -f --output=cat",
        "on-click-right": "systemctl --user stop ethminer",
        "format": "{} ﲹ"
    },
    "sway/workspaces": {
        "format": "{icon} {name}",
        "format-icons": {
            "focused": "綠",
            "default": "祿"
        }
    },
    "tray": {
        "icon-size": 18,
        "spacing": 0
    },
    "clock": {
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
        "format-alt": "{:%d/%m/%Y}"
    },
    "cpu": {
        "format": "{usage}% ",
        "tooltip": false
    },
    "memory": {
        "format": "{}% "
    },
    "temperature#cpu": {
        "hwmon-path": "/sys/class/hwmon/hwmon2/temp1_input",
        "format": "{temperatureC}°C",
        "format-icons": ["", "", "", "", ""]
    },
    "temperature#gpu": {
        "hwmon-path": "/sys/class/hwmon/hwmon1/temp1_input",
        "format": "{temperatureC}°C {icon}",
        "format-icons": ["", "", "", "", ""]
    },
    "network": {
        "format-wifi": "{essid} ",
        "format-ethernet": "{ipaddr} ",
        "format-linked": "(No IP) ",
        "format-disconnected": "Disconnected ⚠",
        "interval": 5
    },
    "pulseaudio": {
        "format": "{volume}% {icon}",
        "format-muted": "0%  ",
        "format-icons": {
            "headphone": "",
            "headset": "",
            "portable": "",
            "default": ["", "", ""]
        },
        "on-click": "pavucontrol"
    }
}
