#!/usr/bin/bash
false
while [ $(echo $?) -ne 0 ]; do
	password=$(zenity --password)
	if [ $(echo $?) -ne 0 ]; then
		exit -1
	fi
	echo $password | sudo --stdin true
done

echo $password | sudo --stdin $*
